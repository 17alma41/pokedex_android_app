<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Sombra -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="20dp"/> <!-- Bordes redondeados -->
            <solid android:color="@color/black"/> <!-- Color de la sombra -->
        </shape>
    </item>

    <!-- Contenedor del Pokémon con bordes -->
    <item android:left="4dp" android:top="4dp" android:right="4dp" android:bottom="4dp">
        <shape android:shape="rectangle">
            <corners android:radius="20dp"/> <!-- Bordes redondeados -->
            <solid android:color="#FFFFFF"/> <!-- Color del fondo -->
            <stroke android:width="3dp" android:color="#FFCC00"/> <!-- Borde naranja -->
        </shape>
    </item>

</layer-list>
